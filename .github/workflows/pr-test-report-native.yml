name: "PR: Test Report Native"
run-name: "${{ github.workflow }} • ${{ github.event.workflow_run.head_branch }} • ${{ github.event.workflow_run.head_commit.message || github.sha }}"

on:
  workflow_run:
    workflows: [ "PR: Test Native" ]
    types:
      - completed

jobs:
  test-report-native:
    name: "Test Report Native"
    if: ${{ github.event.workflow_run.conclusion != 'cancelled' && github.event.workflow_run.event == 'pull_request' }}
    permissions:
      contents: read
      actions: read
      checks: write
    uses: jdheim/toolfetch/.github/workflows/call-test-report-native.yml@main
